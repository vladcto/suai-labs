import 'package:flutter/material.dart';

class TallDishPage extends StatelessWidget {
  const TallDishPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
