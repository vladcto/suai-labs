import 'package:flutter/material.dart';

abstract final class ThemeColors{
  static const bg = Colors.white;
  static const mainContainer = Colors.white;
  static const mainText = Color.fromRGBO(32, 32, 32, 1.0);
  static const system = Color.fromRGBO(47, 46, 46, 1.0);
}
